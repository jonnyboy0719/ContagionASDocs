The function `ToTerrorPlayer` can also be used for [CBaseEntity@][CBaseEntity] instead of an interger `islotindex`.

Example of usage:  
```cpp
CTerrorPlayer@ pAttacker = ToTerrorPlayer( pEntity );
```

